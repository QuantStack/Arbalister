name: Enforce PR label

on:
  pull_request:
    types: [labeled, unlabeled, opened, edited, synchronize]

jobs:
  pr-title:
    name: Check conventional commit PR title
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Setup Pixi
        uses: prefix-dev/setup-pixi@v0.9.3
        with:
          pixi-version: v0.60.0
          cache: true
          manifest-path: pyproject.toml
      - name: Verify conventional commit conventions on PR title
        run: echo "${{ github.event.pull_request.title }}" | pixi run commitlint
        # TODO: we have not added the labels but we can add them automatically
        # from conventional commit titles
        # pr-label:
        #   name: Check PR label
        #   runs-on: ubuntu-latest
        #   permissions:
        #     pull-requests: write
        #   steps:
        #     - name: enforce-triage-label
        #       uses: jupyterlab/maintainer-tools/.github/actions/enforce-label@v1
